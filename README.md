# AI Pomodoro Clock

一个使用 Python + Streamlit 构建的 AI 番茄时钟项目。

## 当前进度
- 已完成：Stage 0 项目初始化
- 待完成：计时核心、每日统计、AI 学习建议

## 快速开始
1. 创建虚拟环境并安装依赖
2. 复制 `.env.example` 为 `.env` 并填入 API Key
3. 运行 `streamlit run app.py`
